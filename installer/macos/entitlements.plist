<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements for unsigned distribution.
  The app will trigger Gatekeeper on first launch.
  Users must right-click → Open, or run:
    xattr -d com.apple.quarantine NexusMonitor.app

  For signed distribution, add a Developer ID Application certificate
  and use codesign --deep --force --options runtime --entitlements ...
-->
<plist version="1.0">
<dict>
    <!-- .NET runtime requires JIT and unsigned executable memory -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow dylib injection (needed by .NET diagnostics) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
