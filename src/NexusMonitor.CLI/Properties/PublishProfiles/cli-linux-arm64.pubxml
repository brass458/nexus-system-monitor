<?xml version="1.0" encoding="utf-8"?>
<!--
  Linux ARM64 CLI — Self-contained portable build (Raspberry Pi 4+, AWS Graviton, etc.)
  Usage: dotnet publish src/NexusMonitor.CLI /p:PublishProfile=cli-linux-arm64
  Output: src/NexusMonitor.CLI/publish/cli-linux-arm64/
  Package: tar -czf NexusCLI-linux-arm64.tar.gz -C publish/cli-linux-arm64 .
-->
<Project>
  <PropertyGroup>
    <Configuration>Release</Configuration>
    <Platform>Any CPU</Platform>
    <RuntimeIdentifier>linux-arm64</RuntimeIdentifier>
    <TargetFramework>net8.0</TargetFramework>
    <SelfContained>true</SelfContained>
    <PublishReadyToRun>false</PublishReadyToRun>
    <PublishTrimmed>false</PublishTrimmed>
    <PublishSingleFile>false</PublishSingleFile>
    <PublishDir>publish/cli-linux-arm64/</PublishDir>
    <DebugType>none</DebugType>
    <DebugSymbols>false</DebugSymbols>
    <StripSymbols>true</StripSymbols>
    <!-- Strip WINDOWS if cross-compiling from a Windows host -->
    <DefineConstants>$(DefineConstants.Replace(';WINDOWS', '').Replace('WINDOWS;', '').Replace('WINDOWS', ''));LINUX</DefineConstants>
  </PropertyGroup>
</Project>
